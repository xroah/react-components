import { styled } from "styled-components"

const Overlay = styled.div`
        position: absolute;
        width: 100%;
        left: 0;
        top: 0;
        z-index: 1000;
    `

export default Overlay