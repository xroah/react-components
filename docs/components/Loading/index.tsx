import * as React from "react"
import { Spinner } from "reap-ui"

export default () => (
    <Spinner
        animation="border"
        className="component-loading-spinner"
        variant="primary" />
)