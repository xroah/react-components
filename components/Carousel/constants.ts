export const PREFIX = "carousel"
export const ITEM_PREFIX = `${PREFIX}-item`

export const START_CLASS = `${ITEM_PREFIX}-start`
export const END_CLASS = `${ITEM_PREFIX}-end`
export const NEXT_CLASS = `${ITEM_PREFIX}-next`
export const PREV_CLASS = `${ITEM_PREFIX}-prev`